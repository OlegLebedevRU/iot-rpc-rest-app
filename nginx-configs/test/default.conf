auth_jwt_enabled off;
    #resolver 127.0.0.11;
    #upstream app {
    #server app1:3031;
    #}
    server {
      listen 80;

      location / {
        return 200 "test";
        #proxy_pass http://app;
      }
    }