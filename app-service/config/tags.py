# config/tags.py

# Ссылки на документацию
DOCS_TASK_WORKFLOW = "https://github.com/OlegLebedevRU/iot-rpc-rest-app/blob/master/docs/1-task-workflow-doc.md"
DOCS_EVENTS_FORMAT = "https://github.com/OlegLebedevRU/iot-rpc-rest-app/blob/master/docs/2-events-api-format-description.md"
DOCS_WEBHOOKS = (
    "https://github.com/OlegLebedevRU/iot-rpc-rest-app/blob/master/docs/3-webhooks.md"
)

# Описание тегов с использованием констант
TAGS_METADATA = [
    {
        "name": "Device tasks",
        "description": f"""
# 📦 Управление задачами

Отправка команд устройствам и получение результатов.

- Создание задач (например, выполнение команд)
- Получение статуса задачи
- Просмотр истории задач по `device_id`
- Поддержка пагинации

> Все операции требуют `x-api-key` и проверяют принадлежность устройства к организации.

📄 [Полная документация по управлению заданиями]({DOCS_TASK_WORKFLOW})
        """,
    },
    {
        "name": "Device events",
        "description": f"""
# 🔔 События с устройств

Получение событий, генерируемых устройствами (например, телеметрия, датчики, состояние).

### Доступные эндпоинты:
- `/events/` — пагинированный список событий по `device_id`
- `/events/incremental` — строго инкрементальная выборка (для синхронизации)
- `/events/fields` — агрегация данных по полям (например, температура за период)

> Поддерживает фильтрацию, лимиты и временные интервалы.

📄 [Документация по форматам событий]({DOCS_EVENTS_FORMAT})
        """,
    },
    {
        "name": "Devices",
        "description": """
# 💡 Устройства

Работа с реестром устройств организации.

### Функции:
- Получение списка устройств с их статусом
- Привязка `tags` к устройству (например, `location`, `zone`)
- Поиск по `device_id`

> Доступ только к устройствам своей организации.
        """,
    },
    {
        "name": "Gauges",
        "description": """
# 📊 Метрики и датчики (Gauges)

Получение показаний датчиков и метрик состояния оборудования.

### Возможности:
- Пагинированный список показаний датчиков
- Фильтрация по `device_id` и типу метрики
        """,
    },
    {
        "name": "Webhooks",
        "description": f"""
# 🌐 Вебхуки

Управление вебхуками для интеграции событий с внешними системами.

- Поддерживаемые типы: `msg-event`, `msg-task-result`
- Организация определяется по `x-api-key`
- Максимум вебхуков на организацию: 2

📄 [Полная документация по вебхукам]({DOCS_WEBHOOKS})
        """,
    },
]
