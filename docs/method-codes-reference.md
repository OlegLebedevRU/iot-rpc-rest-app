# 📘 Справочник MQTT RPC `method_code`

Этот документ — единый реестр всех `method_code`, используемых в приложении. Он объединяет константы протокола и связанные описания из документации по потокам данных.

> 📡 **Форматы применимы как для MQTT RPC, так и для REST-API.** Поля `payload.dt`, `method_code` и структуры ответов `res` идентичны в обоих случаях. Единственное отличие — формат **конверта-обёртки**: для MQTT он описан в [`mqtt-rpc-protocol.md`](./mqtt-rpc-protocol.md), для REST — в [`1-task-workflow-doc.md`](./1-task-workflow-doc.md).

> 📋 **Детальное описание форматов конверта, структуры сообщений и способов передачи мета-данных через UserProperty MQTT5** — см. [`mqtt-rpc-protocol.md`](./mqtt-rpc-protocol.md).

## 🧭 Что важно помнить

- `method_code` определяет действие, которое устройство должно выполнить в рамках RPC-запроса от сервера.
- Для всех сообщений обязательна сквозная передача `correlationData`.
- Если ответ `/res` не содержит специальных полей, используется стандартный ответ:

```json
{
  "status": "OK",
  "method_code": 49
}
```

---

## 🗂️ 1. Диапазоны и назначение кодов

| Диапазон / код | Тип | Назначение |
| :-- | :-- | :-- |
| `1..2999` | ⚙️ Стандартные | Обрабатываются во внутренних подсистемах прошивки ESP32: OTA, NVS, UART, загрузка и сброс данных |
| `3000..3999` | 🌐 Интерактивные | Сквозная передача запросов во внешние шлюзы через WebSocket |
| `0xFFFF` / `65535` | 🚨 Терминальный | Fail-fast код для завершения вызова при ошибке структуры или валидации |

> ⚠️ Для интерактивных `method_code` из диапазона `3000..3999` устройство обязано завершать вызов в терминальное состояние, то есть сразу публиковать `/res` со статусом `500`, даже если внешний WS-клиент недоступен.

---

## 🧾 2. Реестр `method_code`

Ниже приведена сводная таблица `method_code`, отражённых в документации.

| `method_code` | Константа | Описание | Связанная документация |
| :-- | :-- | :-- | :-- |
| `15` | `CMD_LOAD_CELLS_FROM_CLOUD` | Загрузка конфигурации ячеек из облака | — |
| `16` | `CMD_LOAD_CARDS_FROM_CLOUD` | Загрузка списка RFID-карт / доступов из облака | — |
| `17` | `CMD_LOAD_CONFIG_FROM_CLOUD` | Загрузка общей конфигурации платформы в устройство | — |
| `18` | `CMD_SEND_485` | Отправка сырых данных или команды в шину RS-485 | — |
| `19` | `CMD_SW_UPDATE` | OTA-обновление прошивки ESP32 | `ota.md`, `solution-presentation.md`, `thingsboard-comparison.md` |
| `20` | `CMD_SUB_REQUEST` | Выполнение суб-запросов (`batching`) | — |
| `21` | `CMD_ESP_RESTART` | Программная перезагрузка контроллера ESP32 | — |
| `22` | `CMD_SEND_LOG_RECORD` | Принудительная отправка логов | — |
| `26` | `CMD_ERASE_CARDS` | Очистка всех ключей / карт на устройстве | — |
| `34` | `CMD_ERASE_LOG` | Очистка буфера лог-записей | — |
| `35` | `CMD_API_CODE_INPUT` | Генерация программного ввода кода / доступа | `api.md`, `l4_input_frontend.md`, `thingsboard-comparison.md` |
| `42` | `CMD_ASCII_TO_UART` | Отправка ASCII-строки напрямую в UART | — |
| `47` | `CMD_DELETE_CARDS` | Удаление отдельных ключей / карт доступа | — |
| `48` | `CMD_SEND_RAW` | Отправка raw-байтов в порт | — |
| `49` | `CMD_SET_NVS_RECORD` | Запись значений конфигурации в NVS-хранилище | `api.md`, `frontend_ops_cheatsheet.md`, `mqtt-rpc-protocol.md`, `thingsboard-comparison.md` |
| `50` | `CMD_GET_NVS_RECORD` | Чтение значений конфигурации из NVS, возвращает массив | `api.md`, `mqtt-rpc-protocol.md`, `thingsboard-comparison.md` |
| `51` | `CMD_OPERATE_CELL` *(используется в проекте)* | Управление ячейкой / замком / актуатором (однократный адресный сигнал в стек аппаратного контроллера) | `mqtt-rpc-protocol.md` |
| `411` | `CMD_STM32_MCU_UPDATE` | Обновление прошивки сопроцессора STM32 | `stm32_hw.md`, `thingsboard-comparison.md` |
| `512` | `CMD_STM32_BIN_DOWNLOAD` | Скачивание бинарного образа для STM32 с запуском задачи | `stm32_bin_download.md`, `thingsboard-comparison.md` |
| `3000..3999` | *(интерактивные коды)* | Роутинг для локальных шлюзов / WS-клиентов | `mqtt-rpc-client-flow.md`, `thingsboard-comparison.md`, `ws-formats.md` |
| `65535` (`0xFFFF`) | `CMD_INVALID_JSON` | Ошибка структуры / валидации, fail-fast завершение | `mqtt-rpc-client-flow.md`, `thingsboard-comparison.md` |

---

## 🧩 3. Форматы `payload.dt` и ответы `res`

### 🔹 `15` — `CMD_LOAD_CELLS_FROM_CLOUD`
- **Запрос `dt`:** формат зависит от логики парсинга ячеек; ожидается массив объектов со свойствами ячеек.

### 🔹 `16` — `CMD_LOAD_CARDS_FROM_CLOUD`
- **Запрос `dt`:** ожидаются объекты привязок карт:

```json
[
  {
    "cd": "номер_карты",
    "cl": 1
  }
]
```

### 🔹 `18` — `CMD_SEND_485`
- **Запрос `dt`:**

```json
[
  {
    "uartNum": 2,
    "txBase64": "base64_string"
  }
]
```

- **Назначение:** отправка бинарных данных в указанный UART / RS-485.

### 🔹 `19` — `CMD_SW_UPDATE`
- **Запрос `dt`:**

```json
[
  {
    "url": "https://..."
  }
]
```

- **Назначение:** запуск OTA-обновления ESP32.
- **Ошибки `res`:** `400` — невалидный URL или пустой объект, `409` — OTA уже выполняется, `500` — внутренняя ошибка.

### 🔹 `20` — `CMD_SUB_REQUEST`
- **Запрос `dt`:**

```json
[
  {
    "mt": 0
  }
]
```

- **Назначение:** пакетные команды / sub-events. Генерирует исходящие события `evt` со статусом `206` во внутреннюю шину.
- **Поддерживаемые события:** `0` (`HELLO_FROM_DEV`), `4` (`COPY_DATA_CELLS_TO_CLOUD`), `5`, `25` (`SEARCH_CARDS_BY_CELL`, с полем `"cl"`), `28`, `37`.

### 🔹 `21` — `CMD_ESP_RESTART`
- **Запрос `dt`:** может быть пустым, значение игнорируется.
- **Назначение:** мгновенный вызов `esp_restart()`.

### 🔹 `35` — `CMD_API_CODE_INPUT`
- **Запрос `dt`:** массив ASCII-строк длиной не более `15` символов.

```json
[
  "12345",
  "67890"
]
```

- **Назначение:** генерация программных событий чтения карт / кодов.
- **Ответ `res`:** для каждой строки формируется индивидуальный результат со `status_code`: `200` (`accepted`), `423` (`dropped_invalid` или `dropped_busy`).

### 🔹 `42` — `CMD_ASCII_TO_UART`
- **Запрос `dt`:** публикует переданные в поле `"tx"` строки напрямую на внешний интерфейс.
- **Примечание:** ожидается расширение стандарта вида `"tx": "строка"`.

### 🔹 `48` — `CMD_SEND_RAW`
- **Запрос `dt`:**

```json
[
  {
    "type": "udp",
    "port": 62778,
    "txBase64": "base64_string"
  }
]
```

### 🔹 `49` — `CMD_SET_NVS_RECORD`
- **Запрос `dt`:**

```json
[
  {
    "p": "cfg",
    "ns": "cfg_eth",
    "k": "eth_port_mode",
    "t": "i32",
    "v": 0
  }
]
```

- **Примечание:** подробности и типы (`str`, `i32` и др.) описаны в `mqtt-rpc-protocol.md`.
- **Ответ `res`:**

```json
{
  "status": "OK",
  "method_code": 49,
  "records_updated": 1
}
```

### 🔹 `50` — `CMD_GET_NVS_RECORD`
- **Запрос `dt`:**

```json
[
  {
    "ns": "cfg_eth"
  }
]
```

- **Ответ `res`:** возвращает сырой массив записей:

```json
[
  {
    "k": "...",
    "t": "...",
    "v": "..."
  }
]
```

### 🔹 `51` — `CMD_OPERATE_CELL`
- **Назначение:** управление ячейкой / замком / актуатором (однократный адресный сигнал в стек аппаратного контроллера, который осуществляет физическое исполнение команды).
- **Запрос от сервера:** должен включать параметр `cl` в массиве `dt`.

```json
[
  {
    "cl": 1
  }
]
```

- **Ожидаемый ответ `/res`:** должен явно подтверждать обработку конкретной ячейки.

```json
{
  "cl": 1,
  "ts": "2026-04-20T13:17:30.798282+00:00",
  "result": "ok",
  "accepted": true,
  "method_code": 51
}
```

### 🔹 `411` — `CMD_STM32_MCU_UPDATE`
- **Назначение:** запуск обновления прошивки сопроцессора STM32 по шине из локальной базы.

### 🔹 `512` — `CMD_STM32_BIN_DOWNLOAD`
- **Запрос `dt`:**

```json
[
  {
    "url": "https://...",
    "sha256": "64_hex_chars_hash",
    "chunk_size": 1024
  }
]
```

- **Ответ `res`:** при успешном старте:

```json
{
  "status": "OK",
  "method_code": 512,
  "detail": "download task started"
}
```

- **Ошибки `res`:** `400` — плохие аргументы, `409` — загрузка уже выполняется.

### 🔸 `3000..3999` — интерактивные коды
- **Назначение:** сквозные вызовы, где `payload` и структура определяются подключённым клиентом.
- **Особенность:** fail-fast при недоступности WS-канала.

---

## ✅ 4. Краткие правила для интеграции

- Всегда передавайте `correlationData` сквозным образом.
- Для `3000..3999` обрабатывайте недоступность WS-клиента через fail-fast.
- Для ошибок структуры или валидации используйте `CMD_INVALID_JSON` (`65535` / `0xFFFF`).
- Если команда не требует специального ответа, возвращайте стандартный `res` с `status` и `method_code`.

---

## 📚 5. Справочник связанных документов по RPC

| Файл | Назначение |
| :-- | :-- |
| [`mqtt-rpc-protocol.md`](./mqtt-rpc-protocol.md) | Детальное описание протокола MQTT RPC v5: форматы конверта, структура топиков, UserProperty, correlationData, паттерн запрос/ответ |
| [`1-task-workflow-doc.md`](./1-task-workflow-doc.md) | REST-специфичный документ: workflow задач через REST-API, форматы HTTP-запросов и ответов, жизненный цикл задачи |
| [`correlation-data-guide.md`](./correlation-data-guide.md) | Руководство по передаче и обработке `correlationData` / `correlation_id` во всех клиентах и транспортах |
| [`mqtt-rpc-client-flow.md`](./mqtt-rpc-client-flow.md) | Диаграммы потоков MQTT RPC: сценарии запрос–ответ, обработка ошибок, интерактивные коды `3000..3999` |
| [`mqtt-rpc-correlation-matrix.md`](./mqtt-rpc-correlation-matrix.md) | Сводная матрица: где и как передаётся `correlationData` в протоколе и текущей реализации |
| [`event-types-reference.md`](./event-types-reference.md) | Реестр типов событий (`evt`), генерируемых устройством |
| [`2-events-api-format-description.md`](./2-events-api-format-description.md) | Форматы событий, принимаемых через API со стороны сервера |
| [`server-integration-guide.md`](./server-integration-guide.md) | Руководство по интеграции серверной стороны с IoT RPC |
